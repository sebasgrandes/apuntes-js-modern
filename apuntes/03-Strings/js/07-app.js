const producto = "Monitor 20 Pulgadas";
console.log(producto.toUpperCase());

const email = "sEbas543an42C@gMail.CoM";
console.log(email.toLowerCase());

const numero = 598; // como es un numero aparece en azul
console.log(numero);
console.log(numero.toString()); // como es un string aparece de negro
